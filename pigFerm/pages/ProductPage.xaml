<Page x:Class="pigFerm.pages.ProductPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pigFerm.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductPage">

    <Grid>
        <TabControl>
            <TabItem Header="Продукция">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Button Name="addNewProductBtn" Click="addNewProductBtn_Click" Content="Добавить продукцию"/>

                    <ListView Name="productLV" Grid.Row="1" Grid.IsSharedSizeScope="True">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition SharedSizeGroup="idCol"/>
                                        <ColumnDefinition SharedSizeGroup="prodTypeCol"/>
                                        <ColumnDefinition SharedSizeGroup="quantityCol"/>
                                        <ColumnDefinition SharedSizeGroup="dateCol"/>
                                        <ColumnDefinition SharedSizeGroup="dateCol"/>
                                        <ColumnDefinition SharedSizeGroup="costCol"/>
                                        <ColumnDefinition SharedSizeGroup="eventcCol"/>
                                        <ColumnDefinition SharedSizeGroup="descCol"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding id}" Grid.Column="0"/>
                                    <TextBlock Text="{Binding productType.nameTypeProduct}" Grid.Column="1"/>
                                    <TextBlock Text="{Binding quantityUnit}" Grid.Column="2"/>

                                    <StackPanel Grid.Column="3">
                                        <TextBlock Text="Произведен: "/>
                                        <TextBlock Text="{Binding prodauctionDate, StringFormat='dd.MM.yyyy'}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="Годен до: "/>
                                        <TextBlock Text="{Binding expirationDate, StringFormat='dd.MM.yyyy'}"/>
                                    </StackPanel>

                                    <TextBlock Text="{Binding costUnit}" Grid.Column="5"/>

                                    <TextBlock Text="{Binding eventNameAndDate}" Grid.Column="6"/>
                                    <TextBlock Text="{Binding descriiption}" Grid.Column="7"/>
                                    <Line StrokeThickness="2" Stroke="Black" Width="auto"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </TabItem>
            <TabItem Header="Виды продукции">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Button Name="addNewProductTypeBtn" Click="addNewProductTypeBtn_Click" Content="Добавить вид продукции"/>

                    <ListView Name="ProductTypeLV" Grid.Row="1"  Grid.IsSharedSizeScope="True">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition SharedSizeGroup="idCol"/>
                                        <ColumnDefinition SharedSizeGroup="nameCol"/>
                                        <ColumnDefinition SharedSizeGroup="unitCol"/>
                                        <ColumnDefinition SharedSizeGroup="costCol"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding id}" Grid.Column="0"/>
                                    <TextBlock Text="{Binding nameTypeProduct}" Grid.Column="1"/>
                                    <TextBlock Text="{Binding unit}" Grid.Column="2"/>
                                    <TextBlock Text="{Binding cost}" Grid.Column="3"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </TabItem>
            <TabItem Header="Запасы продукции">
                <ListView Name="reservProductLV" Grid.IsSharedSizeScope="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="{Binding brush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition SharedSizeGroup="idCol"/>
                                    <ColumnDefinition SharedSizeGroup="prodTypeCol"/>
                                    <ColumnDefinition SharedSizeGroup="quantityCol"/>
                                    <ColumnDefinition SharedSizeGroup="dateCol"/>
                                    <ColumnDefinition SharedSizeGroup="dateCol"/>
                                    <ColumnDefinition SharedSizeGroup="costCol"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding id}" Grid.Column="0"/>
                                <TextBlock Text="{Binding productType.nameTypeProduct}" Grid.Column="1"/>
                                <TextBlock Text="{Binding quantityUnit}" Grid.Column="2"/>

                                <StackPanel Grid.Column="3">
                                    <TextBlock Text="Произведен: "/>
                                    <TextBlock Text="{Binding prodauctionDate, StringFormat='dd.MM.yyyy'}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="4">
                                    <TextBlock Text="Годен до: "/>
                                    <TextBlock Text="{Binding expirationDate, StringFormat='dd.MM.yyyy'}"/>
                                </StackPanel>

                                <TextBlock Text="{Binding costUnit}" Grid.Column="5"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
